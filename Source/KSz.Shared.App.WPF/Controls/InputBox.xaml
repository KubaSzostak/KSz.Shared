<Window x:Class="System.Windows.InputBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="?" MaxWidth="600"
        MinWidth="300"
        mc:Ignorable="d" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        SizeToContent="WidthAndHeight" ResizeMode="NoResize" 
        ShowInTaskbar="False" WindowStartupLocation="CenterOwner" 
        >
    <Grid Margin="16,8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="8">
            <Label Content="Question" Name="QuestionLabel"  />
            <TextBox Name="AnswerTextBox" />
            <Separator Margin="8" />
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="75" Margin="8" Name="OKButton" Click="OKButton_Click" />
            <Button Content="Anuluj" Width="75" Margin="8" Name="CancelButton" Click="CancelButton_Click" />            
        </StackPanel>
    </Grid>
</Window>
